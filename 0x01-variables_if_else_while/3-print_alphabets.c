#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * return: 0 (Success)
 */
int main(void)
{ 
	char alphabet = 'a';
	while (a <= 'z')
	{
		putchar(alphabet);
		a++;
	}
	
	alphabet = 'A';

	while (alphabet <= 'Z')
	{
		putchar(alphabet);
		a++;
	}
	
	putchar('\n');
	return (0);
}
