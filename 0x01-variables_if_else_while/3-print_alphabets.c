#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * return: 0 (Success)
 */
int main(void)
{ 
	char alphabet;

	for (alphabet = 'A'; alphabet <= 'Z' ; alphabet++)
	putchar (alphabet);
	putchar('\n');
	return (0);
