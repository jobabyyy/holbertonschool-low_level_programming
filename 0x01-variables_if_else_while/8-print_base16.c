#include <stdio.h>
/**
 * main - print numbers of base 16 in lower case
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 48; a < 58; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
