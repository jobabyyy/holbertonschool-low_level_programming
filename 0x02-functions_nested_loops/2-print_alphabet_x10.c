#include "main.h"
/**
 * print_alphabet_x10 - use _putchar to print in
 * lowercase 10xs
 * Return: Always 0 (Success)
 *
 */
void print_alphabet_x10(void)
{
	int o
	int l;

	l = 0;
	while (l < 10)
	{
	for (o = 'a' ; o <= 'z'; o++)
	}
	_putchar(o);
}

l++;
_putchar('\n');
}

}
